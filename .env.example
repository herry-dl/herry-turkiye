# Google Cloud: Maps JavaScript API etkin olmalı.
# Üretimde anahtarı HTTP referrer ile kısıtla (ör. https://*.github.io/herry-turkiye/*).
#
# GitHub Actions ile deploy: repo → Settings → Secrets → Actions →
# New repository secret: adı tam olarak VITE_GOOGLE_MAPS_API_KEY olsun.
VITE_GOOGLE_MAPS_API_KEY=
